// List of names
let namesArray: string[] = ["Milad", "Saad", "Anas"];

// Send dinner invitations
let invitedPerson = namesArray.map((personName, i) => {
  console.log(`${personName} ${invite_message}.`);
});
