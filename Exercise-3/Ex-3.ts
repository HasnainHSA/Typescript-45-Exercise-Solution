// Declaring a constant 
const authorName: string = "Allama Iqbal’s";

// Declaring a variable
let quote: string = "Spend money to make character but don ‘t spend character to make money";

// Printing the formatted quote using template literals
console.log(`${authorName} once said, '${quote}'`);
